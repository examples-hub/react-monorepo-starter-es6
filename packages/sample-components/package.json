{
  "name": "@examples-hub/sample-components",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/index.js",
  "module": "src/index.js",
  "scripts": {
    "build": "rimraf dist && npm run build:es6",
    "build:es6": "rimraf dist && babel src --out-dir dist --root-mode upward --ignore 'src/**/*.test.js','src/**/*.stories.js' --source-maps --verbose",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@examples-hub/package-a": "1.0.0"
  }
}
